import config from "@/lib/config"

export default function AIUse() {
  return <article className="prose prose-stone max-w-none"><h1>AI use</h1><p>{config.app.title} offers an optional workspace-level AI extraction feature for PDF and image documents.</p><h2>What is sent</h2><p>When an owner enables AI extraction, the document is sent over an encrypted connection to <a href="https://mineru.net" rel="noreferrer">MinerU</a>, a document-parsing service, which reads it — including scans, photographs, and handwriting — and returns its text as markdown. That text, together with the selected template’s extraction instructions, is then sent to the platform-configured model, which returns structured values for the template fields. The model receives text only; the document itself is never sent to it.</p><p>Nothing leaves the platform for a workspace with AI extraction switched off.</p><h2>Your controls</h2><p>Owners can disable AI extraction. Documents can still arrive and be entered or corrected manually. AI output is clearly separate from the values a workspace member reviews and saves.</p><h2>Retention</h2><p>Source files are retained for the life of the workspace. The service does not log document bodies, base64 payloads, or prompts containing document data. Questions can be sent to <a href={`mailto:${config.app.supportEmail}`}>{config.app.supportEmail}</a>.</p><h2>Accuracy</h2><p>AI output can be incomplete or incorrect. Review it before using exported data.</p></article>
}
