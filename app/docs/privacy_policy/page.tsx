import config from "@/lib/config"

export default function PrivacyPolicy() {
  return <article className="prose prose-stone max-w-none"><h1>Privacy</h1><p>{config.app.title} processes invoices, receipts, and other PDF/image documents for the workspace that uploads them.</p><h2>Data we retain</h2><ul><li>Account and workspace access data</li><li>Template definitions and workspace settings</li><li>Uploaded source documents, extracted structured values, and member-reviewed corrections</li><li>Minimal, non-sensitive audit events such as document received or reviewed</li></ul><h2>Source retention</h2><p>Uploaded source documents and their structured extraction are retained for the life of the workspace so they remain viewable, searchable, and exportable.</p><h2>Security</h2><p>Sources are held in private encrypted storage, browser access is authorized through the application, and workspace actions require membership. We do not intentionally log document contents, model prompts containing document data, or base64 source payloads.</p><h2>AI processing</h2><p>Where a workspace enables AI extraction, document pages and its template instructions are sent to the platform’s configured AI provider as described in the <a href="/docs/ai">AI use notice</a>.</p><h2>Contact</h2><p>For privacy questions contact <a href={`mailto:${config.app.supportEmail}`}>{config.app.supportEmail}</a>.</p></article>
}
